<nav class="navbar navbar-expand-lg navbar-dark navbar-contrast bg-ACCR fixed-top">
  <div class="container-fluid">
    <span><%= link_to 'AMSTR', dashboard_dash_path, class: "navbar-brand" %></span>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <%# If user, show account, else sign in %>
      <% if user_signed_in? %>
      <div class="container-fluid d-md-flex justify-content-between">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
          <%= link_to_unless_current("Home", dashboard_dash_path, { class: "nav-link" }) do %>
            <a class="bd-highlight nav-link active" aria-current="page" href="#">Home</a>
          <% end %>
          </li>
          <li class="nav-item">
          <%= link_to_unless_current("Log Hours", { controller: "worklogs" }, { class: "nav-link" }) do %>
            <a class="nav-link active" aria-current="page" href="#">Log Hours</a>
          <% end %>
          </li>
          <li class="nav-item">
          <%= link_to_unless_current("View Statistics", dashboard_view_stats_path, { class: "nav-link" }) do %>
            <a class="nav-link active" aria-current="page" href="#">View Statistic</a>
          <% end %>
          </li>
          <li class="nav-item">
          <%= link_to_unless_current("View Report", dashboard_view_report_path, { class: "nav-link" }) do %>
            <a class="nav-link active" aria-current="page" href="#">View Report</a>
          <% end %>
          </li>
          <li class="nav-item">
          <%= link_to_unless_current("Search Cases", searches_path, { class: "nav-link" }) do %>
            <a class="nav-link active" aria-current="page" href="#">Search Cases</a>
          <% end %>
          </li>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Account
            </a>
            <%# Change to rails "link_to" for the actual links when implementing %>   
            <%# Make sure to include the css class as shown in the login example %>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%# The rails administration panel link %>
              <% if Rails.env.development? %><%= link_to 'Rails Admin', rails_admin_path, class: "dropdown-item" %><% end %>
              <a class="dropdown-item" href="#">Action</a>
              <a class="dropdown-item" href="#">Another action</a>
              <div class="dropdown-divider"></div>
              <%# Devise requires that the logout command be sent using the 'delete' HTTP verb (method) %>
              <%= link_to "Logout", destroy_user_session_path, class: "dropdown-item", method: :delete %>
            </div>
          </li>
        </ul>
      </div>
      <% else %>
      <div class="container-fluid d-md-flex justify-content-end">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= button_to "Login", new_user_session_path, class: "btn btn-dark" %>
          </li>
        </ul>
      </div>
      <% end %>
    </div>
  </div>
</nav>