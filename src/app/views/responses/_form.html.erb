<%= form_with(model: response, local: true) do |form| %>
  <% if response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(response.errors.count, "error") %> prohibited this response from being saved:</h2>

      <ul>
        <% response.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :response_sent %>
    <%= form.check_box :response_sent %>
  </div>
  <br>

  <%= form.label :response_message %>
  <br>
  <%= form.text_area :response_message, style: "width: 100%; height: 30ch;" %>

  <br>

  <div class="field">
    <%= form.label :date_sent %>
    <%= form.date_field :date_sent %>
  </div>
  <br>

  <div class="actions">
    <%= form.submit %>
  </div>
  <br>
<% end %>
